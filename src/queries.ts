export const contact = `{
    contact {
        id, name, email, dateCreated, dateModified
    }
}`

export const contacts = `{
    contacts {
        id, name, email, dateCreated, dateModified
    }
}`

export default {
    contact,
    contacts
}